[package]
name = "shader"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
spirv-std = "0.6.0"
bevy-pbr-rust = { git = "https://github.com/Bevy-Rust-GPU/bevy-pbr-rust", branch = "master" }
rust-gpu-sdf = { path = "../../../../rust-gpu-sdf/crates/rust-gpu-sdf", default_features = false, features = ["spirv-std"] }
